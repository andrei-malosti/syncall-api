services:
  mysql:
    image: mysql:lts
    environment:
      MYSQL_DATABASE: syncalldb
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always

volumes:
  mysql_data:
